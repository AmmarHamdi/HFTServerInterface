# This directory holds TLS certificates generated by scripts/gen_certs.sh.
# The actual *.crt and *.key files are excluded from version control via .gitignore.
# Run `./scripts/gen_certs.sh` to (re-)generate them before starting the server.
